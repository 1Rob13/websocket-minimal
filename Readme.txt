

learnings:
careful with browser cache

PRE-CONDITIONS: Docker installed. (docker -v)

To build run build.sh ( or build both with docker)
to display run : docker compose -f docker-compose.yml up 

in 1 line:

./build.sh && docker compose -f docker-compose.yml up

go into browser: localhost:80